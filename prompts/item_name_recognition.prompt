请从以下信息中识别文档的核心对象名称：

文件名：{file_title}

正文切片（用于辅助识别）：
{context}

要求：
1. 返回内容为字符串形式，只返回一个最核心、最具体的名称；
2. 如果文档是商品、设备、仪器、产品说明书、手册等，优先返回带品牌、型号和名称的完整对象名称；
3. 如果文档不是商品文档，而是制度、方案、标准、论文、报告、项目材料等，请返回该文档的核心主题和关键词名称；
4. 返回结果中不要添加解释、前后缀或其他内容；
5. 如果无法识别明确名称，请返回空字符串。